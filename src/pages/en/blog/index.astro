---
export const prerender = false;

import Layout from '../../../layouts/Layout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import { fetchPublishedPosts } from '../../../lib/notion';

const lang = 'en';

const sortedPosts = await fetchPublishedPosts('en');
---

<Layout title="Blog — AION Growth Studio" lang={lang}>
  <Header lang={lang} />

  <main class="py-16 md:py-24">
    <div class="max-w-7xl mx-auto px-6 md:px-8">
      <div class="max-w-3xl mx-auto text-center mb-16 md:mb-24">
        <h1 class="text-5xl md:text-6xl font-bold text-slate-900 tracking-tight">
          Blog
        </h1>
        <p class="mt-6 text-xl text-slate-500">
          Strategy, technology and intelligence to grow your digital business
        </p>
      </div>

      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
        {sortedPosts.map((post) => (
          <article class="group bg-white rounded-2xl overflow-hidden border border-slate-200 transition-all duration-500 hover:-translate-y-2 hover:shadow-2xl">
            {post.image && (
              <div class="aspect-[16/9] overflow-hidden">
                <img
                  src={post.image}
                  alt={post.title}
                  class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                />
              </div>
            )}

            <div class="p-6 md:p-8">
              {post.category && (
                <span class="inline-block px-3 py-1 bg-slate-100 text-slate-600 text-xs font-medium rounded-full mb-4">
                  {post.category}
                </span>
              )}

              <h2 class="text-xl font-bold text-slate-900 mb-3 group-hover:text-aion-primary transition-colors">
                <a href={`/en/blog/${post.slug}`}>{post.title}</a>
              </h2>

              <p class="text-slate-500 text-sm mb-4 line-clamp-3">
                {post.description}
              </p>

              <div class="flex items-center justify-between text-xs text-slate-400">
                <span>{post.author}</span>
                <time datetime={post.date}>
                  {new Date(post.date).toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'long',
                    day: 'numeric'
                  })}
                </time>
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </main>

  <Footer lang={lang} />
</Layout>

<style>
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
